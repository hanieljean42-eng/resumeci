services:
  - type: web
    name: resumeci
    runtime: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./public
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
